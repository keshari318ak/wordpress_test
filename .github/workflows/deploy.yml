on: 
   pull:
     branches: [ main ]
name: Deploying WordPress Site
jobs:
  build:
    name: wordpress_test
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@main
    - name: Deploy
      uses: rtCamp/action-deploy-wordpress@v2
